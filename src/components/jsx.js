// JSX stands for JavaScript Extention
// JSX allows us to write HTML in React.
// JSX makes it easier to write and add HTML in React.
// Coding JSX : JSX allows us to write HTML elements in JavaScript and place them in the DOM 


export default function JSX()
{
	// Can use let also here .. 
	const name = "Suraj";
	return <h2> Hello I am {name} </h2>
}